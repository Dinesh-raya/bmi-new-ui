# BMI Calculator — Portfolio Edition

Demo: https://<your-vercel-url>

## Stack
- Frontend: static HTML/CSS/JS (Vercel)
- Backend: FastAPI (Render)
- DB: Neon Postgres (optional)

## Quickstart
1. Backend: 
   ```bash
   cd backend
   pip install -r requirements.txt
   uvicorn app.main:app --reload
   ```
2. Frontend: open `frontend/index.html` or host on Vercel.
